<!DOCTYPE html>
<html lang="en" class="mdl-js">

<head>
  <meta http-equiv="Content-Type" content="text/html; charset=UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <link href="assets/favicon.ico" rel="icon">
  <link href="assets/css/bootstrap.min.css" rel="stylesheet">
  <link href="assets/css/styles.css" rel="stylesheet">
  <script src="assets/js/jquery.min.js"></script>
  <script src="assets/js/bootstrap.min.js"></script>
  <title><%= I18n.t :page_title %></title>
</head>

<body>
  <section id="introduction">
    <div class="container">
      <div class="row fill-viewport align-items-center justify-content-center">
        <div class="col-12 col-md-6">
          <div class="row">
            <div class="col-md-auto">
              <h1 class="text-white title"><%= I18n.t :page_title %></h1>
            </div>
          </div>
          <p class="lead text-white-70"> <%= I18n.t :top_players %></p>
          <hr class="mb-4">
          <div class="table-responsive-md scores">
            <table class="table table-striped table table-sm">
              <thead>
                <tr>
                  <th scope="col">#</th>
                  <th scope="col"><%= I18n.t('statistics_header.name') %></th>
                  <th scope="col"><%= I18n.t('statistics_header.level') %></th>
                  <th scope="col"><%= I18n.t('statistics_header.attempts_left') %></th>
                  <th scope="col"><%= I18n.t('statistics_header.hints_left') %></th>
                  <th scope="col"><%= I18n.t('statistics_header.date') %></th>
                </tr>
              </thead>
              <tbody>
                <% unless statistics.nil? %>
                  <% statistics.each.with_index(1) do |user, index| %>
                    <tr>
                      <th scope="row"><%= index %></th>
                      <td><%= user[:name] %></td>
                      <td><%= user[:difficulty] %></td>
                      <td><%= user[:attempts_total] - user[:attempts_used] %>/<%= user[:attempts_total] %></td>
                      <td><%= user[:hints_total] - user[:hints_used] %>/<%= user[:hints_total] %></td>
                      <td><%= user[:date] %></td>
                    </tr>
                  <% end %>
                <% else %>
                <tr>
                  <th colspan="6"><%= I18n.t :no_stats %></th>
                </tr>
                <% end %>
              </tbody>
            </table>
          </div>
          <hr class="mb-4">
          <a class="btn btn-primary btn-lg" href="/" role="button">
            <%= I18n.t('links.home') %>
          </a>
        </div>
       </div>
    </section>
  </body>
</html>
